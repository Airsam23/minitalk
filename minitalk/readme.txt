The minitalk project is a client-server application in C, where the client sends messages as binary signals to the server using UNIX signals (SIGUSR1 and SIGUSR2), and the server decodes these signals to reconstruct the messages and display them on the terminal.

ps -e
top
htop
pgrep firefox
